# Training handler dependencies
# Base image provides: torch, numpy, CUDA libs
# qwen-tts (installed separately in Dockerfile with full deps)

# HuggingFace / Qwen-TTS runtime versions
# Keep these aligned with qwen-tts==0.1.1 so pip does not install one set and then
# immediately downgrade it again when qwen-tts is installed.
transformers==4.57.3
accelerate==1.12.0
safetensors>=0.4.0
huggingface-hub>=0.23.0

# Audio (soundfile used by training handler directly)
soundfile>=0.12.0
librosa>=0.10.0
faster-whisper>=1.1.0
einops>=0.8.0
torchaudio==2.10.0

# R2 / S3 client
boto3>=1.34.0

# HTTP (WorkerReporter callbacks + pod self-termination)
requests>=2.31.0
