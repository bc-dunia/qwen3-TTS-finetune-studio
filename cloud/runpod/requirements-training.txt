# Training handler — ABSOLUTE MINIMUM dependencies
# Base image provides: torch, numpy, CUDA libs
#
# REMOVED to shrink image:
# - runpod SDK (not imported by training_handler.py — saves fastapi/uvicorn/starlette ~200MB)
# - faster-whisper/CTranslate2 (dataset already preprocessed)
# - librosa (audio already 24kHz)
# - qwen-tts extras (gradio, torchaudio, onnxruntime, sox)

# HuggingFace (required by training scripts)
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
huggingface-hub>=0.23.0
einops

# Audio (dataset.py mel_spectrogram)
soundfile>=0.12.0

# R2 / S3 client
boto3>=1.34.0

# HTTP (WorkerReporter callbacks + pod self-termination)
requests>=2.31.0
