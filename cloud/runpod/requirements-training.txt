# Training handler dependencies — MINIMAL for smallest Docker image
# The base image (runpod/pytorch) already provides: torch, numpy, CUDA libs
#
# Key size optimizations:
# - qwen-tts installed with --no-deps (avoids gradio, torchaudio, onnxruntime, sox)
# - No faster-whisper/CTranslate2 (preprocessing skipped if train_raw.jsonl exists)
# - No librosa (only needed for resampling, our audio is already 24kHz)

# RunPod SDK (~small)
runpod>=1.7.0

# HuggingFace stack (required by training scripts)
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
huggingface-hub>=0.23.0
einops

# Audio processing (~small, needed by dataset.py mel_spectrogram)
soundfile>=0.12.0

# R2 / S3 client
boto3>=1.34.0

# HTTP client (WorkerReporter + pod termination)
requests>=2.31.0
